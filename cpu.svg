<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="198" onload="init(evt)" viewBox="0 0 1200 198" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
	}

	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom();
			zoom(target);
		}
		else if (e.target.id == "unzoom") unzoom();
		else if (e.target.id == "search") search_prompt();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
	}, false)

	// functions
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
		return;
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		// Fit in full text width
		if (/^ *$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
			return;

		for (var x = txt.length - 2; x > 0; x--) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
	}
	function unzoom() {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			update_text(el[i]);
		}
	}

	// search
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)", "");
			if (term != null) {
				search(term)
			}
		} else {
			reset_search();
			searching = 0;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		var re = new RegExp(term);
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="198.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >Flame Graph</text>
<text id="details" x="10.00" y="181" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="matched" x="1090.00" y="181" > </text>
<g id="frames">
<g >
<title> mkScalar (18,677,492 samples, 0.25%)</title><rect x="1181.7" y="53" width="2.9" height="15.0" fill="rgb(220,227,30)" rx="2" ry="2" />
<text  x="1184.72" y="63.5" ></text>
</g>
<g >
<title> go (849,368 samples, 0.01%)</title><rect x="1078.3" y="101" width="0.2" height="15.0" fill="rgb(243,228,45)" rx="2" ry="2" />
<text  x="1081.35" y="111.5" ></text>
</g>
<g >
<title> takeByteString (5,731,920 samples, 0.08%)</title><rect x="1184.6" y="53" width="0.9" height="15.0" fill="rgb(254,115,50)" rx="2" ry="2" />
<text  x="1187.64" y="63.5" ></text>
</g>
<g >
<title> mkScalar (5,388,258 samples, 0.07%)</title><rect x="1185.6" y="85" width="0.9" height="15.0" fill="rgb(242,130,48)" rx="2" ry="2" />
<text  x="1188.64" y="95.5" ></text>
</g>
<g >
<title> zipWith (541,050,670 samples, 7.19%)</title><rect x="1105.2" y="117" width="84.8" height="15.0" fill="rgb(225,122,9)" rx="2" ry="2" />
<text  x="1108.21" y="127.5" > zipWith</text>
</g>
<g >
<title> challengeScalar (59,188,718 samples, 0.79%)</title><rect x="1176.3" y="85" width="9.2" height="15.0" fill="rgb(215,210,7)" rx="2" ry="2" />
<text  x="1179.27" y="95.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (1,583,588 samples, 0.02%)</title><rect x="1189.7" y="37" width="0.2" height="15.0" fill="rgb(234,179,15)" rx="2" ry="2" />
<text  x="1192.65" y="47.5" ></text>
</g>
<g >
<title> mkScalar (122,193,489 samples, 1.62%)</title><rect x="1079.0" y="117" width="19.1" height="15.0" fill="rgb(210,123,43)" rx="2" ry="2" />
<text  x="1081.98" y="127.5" ></text>
</g>
<g >
<title> transcriptNew (9,908,550 samples, 0.13%)</title><rect x="1103.7" y="117" width="1.5" height="15.0" fill="rgb(236,48,48)" rx="2" ry="2" />
<text  x="1106.66" y="127.5" ></text>
</g>
<g >
<title> go (539,351,934 samples, 7.16%)</title><rect x="1105.5" y="101" width="84.5" height="15.0" fill="rgb(254,166,30)" rx="2" ry="2" />
<text  x="1108.48" y="111.5" > go</text>
</g>
<g >
<title> mkScalar (3,157,631 samples, 0.04%)</title><rect x="1078.5" y="101" width="0.5" height="15.0" fill="rgb(231,36,47)" rx="2" ry="2" />
<text  x="1081.48" y="111.5" ></text>
</g>
<g >
<title> mkScalar (177,019,936 samples, 2.35%)</title><rect x="1037.2" y="85" width="27.7" height="15.0" fill="rgb(214,77,8)" rx="2" ry="2" />
<text  x="1040.16" y="95.5" > ..</text>
</g>
<g >
<title> `$wchallengeScalar` (54,092,510 samples, 0.72%)</title><rect x="1177.1" y="69" width="8.4" height="15.0" fill="rgb(223,192,9)" rx="2" ry="2" />
<text  x="1180.06" y="79.5" ></text>
</g>
<g >
<title>all (7,529,682,987 samples, 100%)</title><rect x="10.0" y="149" width="1180.0" height="15.0" fill="rgb(236,15,10)" rx="2" ry="2" />
<text  x="13.00" y="159.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (23,370,642 samples, 0.31%)</title><rect x="1100.0" y="101" width="3.7" height="15.0" fill="rgb(246,7,34)" rx="2" ry="2" />
<text  x="1102.99" y="111.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (12,477,115 samples, 0.17%)</title><rect x="1101.7" y="85" width="2.0" height="15.0" fill="rgb(249,68,45)" rx="2" ry="2" />
<text  x="1104.70" y="95.5" ></text>
</g>
<g >
<title> takeByteString (78,055,360 samples, 1.04%)</title><rect x="1064.9" y="85" width="12.2" height="15.0" fill="rgb(219,14,51)" rx="2" ry="2" />
<text  x="1067.90" y="95.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (2,375,382 samples, 0.03%)</title><rect x="1103.3" y="69" width="0.4" height="15.0" fill="rgb(240,175,11)" rx="2" ry="2" />
<text  x="1106.29" y="79.5" ></text>
</g>
<g >
<title> `$wchallengeScalar` (459,553,072 samples, 6.10%)</title><rect x="1005.1" y="101" width="72.0" height="15.0" fill="rgb(243,92,5)" rx="2" ry="2" />
<text  x="1008.11" y="111.5" > `$wchal..</text>
</g>
<g >
<title> powerOfTwoExponentiation (21,787,054 samples, 0.29%)</title><rect x="1186.5" y="85" width="3.4" height="15.0" fill="rgb(244,20,49)" rx="2" ry="2" />
<text  x="1189.49" y="95.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (9,851,410 samples, 0.13%)</title><rect x="1188.4" y="53" width="1.5" height="15.0" fill="rgb(242,24,39)" rx="2" ry="2" />
<text  x="1191.36" y="63.5" ></text>
</g>
<g >
<title> challengeScalar (541,722,736 samples, 7.19%)</title><rect x="992.2" y="117" width="84.9" height="15.0" fill="rgb(243,181,38)" rx="2" ry="2" />
<text  x="995.24" y="127.5" > challeng..</text>
</g>
<g >
<title> go (11,770,422 samples, 0.16%)</title><rect x="1077.1" y="117" width="1.9" height="15.0" fill="rgb(205,12,36)" rx="2" ry="2" />
<text  x="1080.13" y="127.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (13,519,232 samples, 0.18%)</title><rect x="1187.8" y="69" width="2.1" height="15.0" fill="rgb(237,163,14)" rx="2" ry="2" />
<text  x="1190.78" y="79.5" ></text>
</g>
<g >
<title> powerOfTwoExponentiation (35,306,286 samples, 0.47%)</title><rect x="1098.1" y="117" width="5.6" height="15.0" fill="rgb(246,141,49)" rx="2" ry="2" />
<text  x="1101.12" y="127.5" ></text>
</g>
<g >
<title>verifyPlonkFast (7,529,682,987 samples, 100.00%)</title><rect x="10.0" y="133" width="1180.0" height="15.0" fill="rgb(230,164,18)" rx="2" ry="2" />
<text  x="13.00" y="143.5" >verifyPlonkFast</text>
</g>
</g>
</svg>
